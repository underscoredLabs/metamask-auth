import { WalletCheckModule, WalletCheckInit, WalletSelectModuleOptions } from '../interfaces';
export default function initializeModules(networkId: number, walletSelect: WalletSelectModuleOptions | undefined, walletCheck: Array<WalletCheckModule | WalletCheckInit> | undefined, isMobile: boolean): {
    walletSelect: {
        heading: string;
        description: string;
        wallets: Promise<any[]>;
        explanation: string;
    };
    walletCheck: Promise<WalletCheckModule[]>;
};
